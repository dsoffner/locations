{
    "<%=page_item%>": [{
            <% if(data.length){
                for(var i = 0;i < data.length;i++) {
                if (i > 0) { %>
                }, {
                <% } else { %>
                <% } %>
                "id": "<%=data[i].id%>",
                "name": "<%=data[i].name%>",
                "address": "<%=data[i].address%>",
                "postcode": "<%=data[i].postcode%>",
                "phone": "<%=data[i].phone%>",
                "email": "<%=data[i].email%>",
                "hours": "<%=data[i].hours%>",
                "map": "<%=data[i].map%>"
                <% }
             } else { %>
            <% } %>
        }]
}
